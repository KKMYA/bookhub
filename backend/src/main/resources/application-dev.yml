spring:
  datasource:
    url: jdbc:sqlserver://localhost:1433;databaseName=bookhub_db;encrypt=true;trustServerCertificate=true
    username: sa
    password: Pa$$w0rd
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true

logging:
  level:
    org.hibernate.SQL: DEBUG
    org.hibernate.type.descriptor.sql.BasicBinder: TRACE

app:
  jwt:
    secret:

